# Failure Log

Append one evidence-backed entry per failure. Required fields: full error summary, category, strategy, fingerprint, same-failure count, root-cause hypothesis, discriminating experiment, minimum repair, rerun result, and resulting state.

Never delete failing tests, weaken acceptance, ignore output, fabricate success, or repeat the same strategy after its third identical failure.
